[build-system]
requires = ["setuptools >= 77.0.3"]
build-backend = "setuptools.build_meta"

[project]
name = "CubeML"
version = "0.1.0"
dependencies = [
    "ipython",
    "scikit-learn",
    "scipy",
    "numpy",
    "pandas",
    "pycuber",
    "opendatasets",
    "matplotlib",
    "tqdm"
]
